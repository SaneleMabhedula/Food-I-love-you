Writer = "Sanele M"

//LIFE
if (Past.Contains(Regrets))
{
    LearnFrom(Past);
    FocusOn(Future);
}

//Family
if(family.expectations>energy.Left)
{
    forget(Myself);
    become(WhoTheyWant);

}

//Value
if (Value<timeGiven)
{
    goodbye();
    /* if they value you less that your time,
    say goodbye. */
}